#ifndef SIMPLEX_H#define SIMPLEX_H#include <stdio.h>typedef struct simplex{  int dim_embedded;  int dim;  double * coord;} simplex;void simplex_free(simplex * s);simplex * simplex_fscan(FILE * in);void simplex_fprint(FILE * out, const simplex * s, const char * format);double simplex_measure(const simplex * s);void simplex_centroid(double * s_medicenter, const simplex * s);#endif /* SIMPLEX_H */