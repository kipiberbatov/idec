#ifndef _double_array_difference_h_
#define _double_array_difference_h_

/* c := a - b (all three are arrays of length n). */
void double_array_difference(
  double * c,
  int n,
  const double * a,
  const double * b);

#endif /* _double_array_difference_h_ */
